Dopt.design <- function(frml, nruns, data=NULL, factor.types=NULL, factor.names=NULL, nlevels=NULL, randomize=TRUE, seed=NULL, ...){
    ## rough sketch of how this will be implemented
    ## calls optFederov
    ## must contruct data from factor.types, factor.names and nlevels
    ## nlevels for quantitative factors can be arbitrary, with factor.names just the scale ends
    ## optFederov(frml, data, nruns, ...)
    cat("This does not work yet.\n")
}
